namespace GameFactory
{
    public enum StartMenuOptions
    {
        SinglePlayer = 1,
        MultiPlayer = 2,
        Options = 3,
        Quit = 4
    }
    public enum OptionsMenu
    {
        NightMode = 1,
        Back = 2
    }
}